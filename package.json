{
  "name": "root",
  "private": true,
  "workspaces": [
    "spent-app",
    "spent-core"
  ],
  "scripts": {
    "start": "yarn wsrun run start",
    "test": "yarn wsrun run test",
    "build-core": "yarn workspace @spent/core build",
    "build-cli": "yarn workspace @spent/cli build",
    "build-all": "yarn run build-core && yarn run build-cli"
  },
  "resolutions": {
    "jss": "^10.0.0-alpha.3"
  },
  "devDependencies": {
    "wsrun": "^3.6.2"
  }
}
