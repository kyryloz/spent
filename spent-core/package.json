{
  "name": "@spent/core",
  "version": "0.0.1",
  "description": "Spent core",
  "private": true,
  "main": "build/index.js",
  "files": [
    "build"
  ],
  "scripts": {
    "build": "yarn clean && tsc --pretty",
    "clean": "rm -rf build",
    "start": "yarn clean && tsc -w --pretty",
    "test": "jest --modulePaths=src --watch"
  },
  "dependencies": {
    "lodash": "^4.17.11",
    "moment": "^2.22.2",
    "nanoid": "^2.0.0",
    "redux": "^4.0.1",
    "reselect": "^4.0.0"
  },
  "devDependencies": {
    "@types/jest": "^23.3.11",
    "@types/lodash": "^4.14.118",
    "@types/nanoid": "^1.2.0",
    "jest": "^23.6.0",
    "prettier": "1.15.3",
    "ts-jest": "^23.10.5",
    "tslint": "^5.11.0",
    "typescript": "^3.1.3"
  },
  "jest": {
    "preset": "ts-jest",
    "testEnvironment": "node",
    "globals": {
      "ts-jest": {
        "tsConfig": "./tsconfig.test.json"
      }
    }
  }
}
